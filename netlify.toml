[build]
  command = "npm run build"
  publish = ".next/static"
  functions = ".next/serverless"

[dev]
  command = "npm run dev"
  port = 3000

[headers]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
